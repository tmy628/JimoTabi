<div class="container">
  <div class="row">
    <div class="col-sm-12">
      <div class="offset-sm-1 mt-5">
        <h2 class="font-weight-bold"><%= t('.sign_in') %></h2>
        <h4 class="pt-4">ユーザーの方はこちらからログイン</h4>
      </div>
      <div class="my-5">
        <%= form_with model: @user, url: user_session_path, local: true do |f| %>

        <div class="field text-center">
          <%= f.label :email, class: "col-2" %>
          <%= f.email_field :email, autofocus: true,  placeholder: "example@example.com", class: "email col-4" %>
        </div>

        <div class="field text-center mt-2">
          <%= f.label :password, class: "col-2" %>
          <%= f.password_field :password, autocomplete: "off", class: "password col-4" %>
        </div>

        <div class="actions text-center my-3">
          <%= f.submit t('.sign_in'), class: "btn btn-primary col-5" %>
        </div>
        <% end %>
      </div>

      <div class = "offset-sm-1">
        <h4>ユーザー登録がお済みでない方</h4>
        <p class = "offset-sm-2 pt-3 mb-5">
          <%= link_to "こちら", new_registration_path(resource_name), class: "underline" %> から新規登録を行ってください。
        </p>
      </div>
    </div>
  </div>
</div>
